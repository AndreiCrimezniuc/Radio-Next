export type Station = {
    id: string,
    name: string,
    site?: string | null,
    email?: string | null,
    region?: string | null,
    city?: string | null,
    address?: string | null,
    facebook?: string | null,
    twitter?: string | null,
    vk?: string | null,
    wiki?: string | null,
    phoneNumber?: string | null,
    stream: string
}